# Bonds

Bonds provides:

- A CLI (`bonds-cli`) for managing symlink-based bonds.
- A Rust library (`bonds-core`) for programmatic integration.

Use the navigation to explore usage guides and API concepts.
For generated Rust API docs, see **Library API -> API Reference**.
